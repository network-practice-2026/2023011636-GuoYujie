# 2023011636-郭宇杰 计算机网络知识体系交互式展示系统

本项目用于《网络实习》课程，主题为“计算机网络知识体系交互式展示系统设计与实现”。系统围绕课程任务要求，提供协议过程动画、综合通信场景模拟、数据库知识库和 3D 知识图谱四个展示模块，适合课程检查、课堂演示和答辩说明。

## 学生信息

- 学号：2023011636
- 姓名：郭宇杰
- 项目名称：计算机网络知识体系交互式展示系统

## 技术栈

- 前端：HTML、CSS、原生 JavaScript
- 后端：Python 标准库 `ThreadingHTTPServer`
- 数据库：SQLite
- 图谱展示：本地 Three.js、SpriteText、3D Force Graph 静态库

项目尽量避免复杂框架和大型状态管理库，优先保证依赖少、易运行、易修改、便于展示。

## 环境配置与依赖

- Python 版本：建议 Python 3.10 及以上
- Python 第三方依赖：无，后端只使用标准库
- 前端第三方库：已放入 `public/vendor/`，无需通过 CDN 或 npm 下载
- 数据库：SQLite，随 Python 标准库 `sqlite3` 使用

因此本项目不需要额外执行 `pip install`、`npm install` 等依赖安装命令。直接启动 `app.py` 即可运行。

## 运行方法

进入项目目录：

```powershell
cd "C:\Users\leyi\Documents\Codex\2026-06-02\files-mentioned-by-the-user-pdf\2023011636-郭宇杰"
```

启动服务：

```powershell
python app.py
```

浏览器访问：

```text
http://127.0.0.1:8000
```

推荐使用本地服务访问，不建议直接打开 `public/index.html`。知识库、知识图谱和数据接口依赖后端服务。

停止服务时，在终端按 `Ctrl+C` 即可。

## 页面结构

系统左侧为目录导航，每个模块是相互独立的页面入口：

- 项目总览
- 一、协议可视化
- 二、模拟过程
- 三、知识库
- 四、知识图谱

点击左侧目录后会切换到对应模块，避免所有内容堆叠在同一个长页面中。

## 一、协议可视化

模块一用于展示基础网络协议或二层机制的动态过程，目前展示 DNS、TCP、交换机转发三个子模块。

### DNS 域名解析

覆盖内容：

- 客户端、DNS 服务器和目标域名输入框
- 用户输入域名后，逐步展示 DNS 查询过程
- 展示缓存命中与缓存未命中
- 展示最终解析出的 IP 地址
- 展示 DNS 缓存表查询和更新结果
- 支持下一步、自动播放、重置、清空缓存

当前 DNS 页面包含两个版本：

- 原版演示：客户端、本地 DNS、根/权威 DNS 的基础解析流程，重点展示缓存命中、缓存未命中、递归/迭代查询和缓存写入。
- 改版演示：客户端与本地 DNS 服务器处于不同网段，中间经过路由器 R。该版本重点展示跨网段通信时 DNS 查询的三层目的 IP 与二层下一跳 MAC 的区别：客户端不能直接发给 DNS 服务器的 MAC，而是先 ARP 默认网关并把 DNS 查询交给路由器转发。

改版演示支持：

- 默认网关 ARP 查询和 ARP 表更新
- 跨网段 DNS Query 的路由转发
- 本地 DNS 缓存命中与缓存未命中
- 本地 DNS 缓存表展示和清空缓存
- 权威 DNS 查询、缓存写入和结果返回

### TCP 连接管理

覆盖内容：

- 客户端和服务器
- 建立连接和释放连接两种演示模式
- SYN、ACK、FIN 等报文动画
- CLOSED、SYN-SENT、ESTABLISHED、FIN-WAIT 等状态变化
- 展示连接建立或释放结果
- 支持下一步、自动播放和重置
- 默认必须先选择“建立连接”或“释放连接”，再执行下一步，避免演示流程含义不明确

### 交换机转发与 MAC 地址表学习

覆盖内容：

- H1、H2、H3、H4 和交换机 S
- 主机发送数据帧过程
- 交换机学习源 MAC 地址过程
- 目的 MAC 未知时泛洪
- 目的 MAC 已知后定向转发
- MAC 地址表动态变化

## 二、模拟过程

模块二根据课程题目给定拓扑，展示 H1 从 t0 时刻访问 `www.abc.com`，到 t1 时刻交换机第一次收到封装 HTTP 请求报文的以太网帧之前的完整通信过程。

拓扑要点：

- H1、H2、本地域名服务器、路由器 R 连接到以太网交换机 S
- 路由器 R 连接 Internet
- Web 服务器位于 Internet 中
- 初始状态下 H1 ARP 表和交换机 S 交换表为空

页面能力：

- 支持开始、上一步、下一步、重播动画、自动播放、重置
- 支持步骤目录跳转
- 按步骤展示当前通信的起点、终点、协议、通信方式
- 展示源 MAC、目的 MAC、源 IP、目的 IP
- 在拓扑图中动态展示帧或报文路径
- 展示交换机 S 的处理行为
- 动态更新 H1 ARP 表
- 动态更新交换机 S 交换表
- 展示协议封装视图，包括应用层、传输层、网络层、数据链路层
- 最终给出从 t0 到 t1 的状态和结论

该模块补充分析了本地域名服务器在没有外部解析缓存时，需要向默认网关 R 进行 ARP 解析并向外部 DNS 查询的过程，因此 t0 到 t1 中也包含这些额外以太网帧对交换机学习表的影响。

## 三、知识库

模块三用于管理 TCP/IP 五层模型知识点，知识点不是写死在页面中，而是存储在 SQLite 数据库中，并通过接口完成增删改查。

主要能力：

- 左侧以 TCP/IP 五层模型目录展示层级关系
- 支持上一层、下一层切换
- 支持按层级展示知识点
- 支持搜索知识点、协议或说明
- 支持新增知识点
- 支持编辑知识点
- 支持删除知识点
- 支持导出 JSON
- 支持“显示英文 / 隐藏英文”切换

英文显示功能示例：

- 应用层（Application Layer）
- 传输层（Transport Layer）
- 网络层（Network Layer）
- 数据链路层（Data Link Layer）
- 物理层（Physical Layer）
- TCP（Transmission Control Protocol）
- DNS（Domain Name System）
- ARP（Address Resolution Protocol）

英文名词仅用于展示，不会写入数据库，不影响新增、编辑、删除和搜索的原始中文数据。

## 四、知识图谱

模块四使用 3D 知识图谱展示计算机网络知识体系。

主要能力：

- 顶层节点为“计算机网络”
- 下一层为 TCP/IP 五层模型
- 再下一层为各层对应协议、设备、技术、机制、概念等知识点
- 点击节点后显示知识点详情
- 支持节点聚焦
- 支持拖动、缩放和旋转视角
- 支持选择显示层数
- 支持重置图谱
- 支持“显示英文 / 隐藏英文”切换

知识库与知识图谱已实现双向联动：

- 在知识库中点击“图谱查看”，会跳转到知识图谱并聚焦对应知识点
- 在知识图谱中点击节点后，可跳转回知识库查看对应条目或对应层级
- 新增、编辑、删除知识点后，知识图谱会读取数据库中的最新知识点

知识图谱数据与知识库取并集，以数据库中当前保存的知识点为主要来源，保证新增知识点也能进入图谱展示。

## 后端接口

后端服务由 `app.py` 提供，主要接口如下：

```text
GET    /api/health
GET    /api/layers
GET    /api/knowledge
POST   /api/knowledge
PUT    /api/knowledge/{id}
DELETE /api/knowledge/{id}
```

其中 `/api/knowledge` 支持参数：

- `layer`：按所属层级筛选
- `q`：按标题、摘要、详情搜索
- `page`：页码
- `page_size`：每页数量

## 数据库

数据库文件：

```text
data/network_knowledge.db
```

建表脚本：

```text
schema.sql
```

数据库初始化和 CRUD 逻辑：

```text
database.py
```

系统启动时会初始化数据库，并写入基础知识点种子数据。

## 文件结构

```text
2023011636-郭宇杰/
├── app.py                         # Python 后端服务入口
├── database.py                    # SQLite 初始化、种子数据和 CRUD 操作
├── schema.sql                     # 数据库建表脚本
├── README.md                      # 项目说明文档
├── .gitignore                     # Git 忽略规则
├── data/
│   └── network_knowledge.db       # SQLite 数据库文件
├── public/
│   ├── index.html                 # 前端页面结构
│   ├── styles.css                 # 页面布局、动画和视觉样式
│   ├── app.js                     # 前端交互、动画、接口调用和图谱逻辑
│   └── vendor/
│       ├── three.min.js           # 3D 渲染库
│       ├── three-spritetext.min.js
│       └── 3d-force-graph.min.js  # 3D 知识图谱库
├── docs/                          # 课程资料或设计文档
├── screenshots/                   # 项目截图目录
├── server.log                     # 本地运行日志
└── server.err.log                 # 本地错误日志
```

运行产物说明：

- `server.log`、`server.err.log` 为本地运行日志
- `__pycache__/` 为 Python 运行缓存
- 这些内容不属于核心源码

## 展示建议

建议按 3 分钟成果汇报控制节奏：

1. 项目总览：说明系统分为协议可视化、综合场景模拟、知识库、知识图谱四个模块。
2. 协议可视化：优先展示 DNS 改版演示和 TCP 建立连接。DNS 改版用于说明跨网段时先发给默认网关，TCP 用于说明 SYN、SYN+ACK、ACK 状态变化。
3. 模拟过程：展示 H1 从 t0 访问 `www.abc.com` 到 t1 时刻交换机 S 第一次收到 HTTP 请求帧的过程，重点说明 ARP 表、交换表、MAC/IP 和协议封装。
4. 知识库：展示 SQLite 知识点查询、新增、编辑、删除或导出 JSON。
5. 知识图谱：展示 3D 节点点击、层级关系和知识库联动。

如果时间紧张，可只演示 DNS 改版、综合场景、知识库查询和知识图谱点击四个关键点。

## 汇报前检查

- 启动服务：`python app.py`
- 浏览器访问：`http://127.0.0.1:8000`
- 确认左侧五个入口可以正常切换
- 确认 DNS、TCP、交换机转发的“开始/下一步/自动播放/重置”可用
- 确认综合模拟可以从步骤 1 前进到最终步骤
- 确认知识库可以查询数据，新增或编辑后刷新仍能保留
- 确认知识图谱可以加载、点击节点并显示详情
- 提前打开 `README.md`、`schema.sql`、`database.py` 和设计文档，方便老师提问时展示

## 项目特点

- 覆盖课程要求中的协议可视化、通信过程拆解、知识库和知识图谱
- DNS 模块包含基础版和跨网段改版，能够展示默认网关、路由转发、缓存命中与缓存未命中
- 使用 SQLite 存储知识点，满足数据化管理要求
- 采用原生前端实现，依赖少，便于修改
- 支持多模块独立页面展示
- 支持逐步播放、自动播放和状态表动态变化
- 支持知识库与知识图谱双向联动
- 支持中英文名词切换，便于答辩说明

## 当前状态

当前版本已完成课程项目主要功能，可通过本地服务直接运行和展示。
